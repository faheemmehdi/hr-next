import CurrentCompensation from "./CurrentComp";
import AllBonuses from "./CurrentComp";

export const metadata = {
  title: "Compensation | HRM",
};

export default function Compensation(){
  return <CurrentCompensation />;
}